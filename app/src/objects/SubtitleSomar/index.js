import styled from "styled-components";

const SubtitleSomar = styled.h2 `
    color: var(--color-zero);
    font-size: var(--size-medium);
    margin-bottom: var(--spacing-big);
    text-align:center;
    width: 500px; 
    position: absolute;
    top:90px;
    left:450px;
`;

export default SubtitleSomar;